export default function IntakePage(){return <main style={{padding:40,fontFamily:'system-ui'}}><h1>Create a cleanup job</h1><p>This MVP accepts a CSV transaction file and sends it to the cleanup packet API.</p><form action="/api/cleanup-jobs" method="post" encType="multipart/form-data" style={{display:'grid',gap:12,maxWidth:640}}><input name="firmName" placeholder="Firm name" style={{padding:12}}/><input name="contactEmail" placeholder="Contact email" style={{padding:12}}/><input name="clientName" placeholder="Client name" style={{padding:12}}/><select name="cleanupType" defaultValue="standard" style={{padding:12}}><option value="light">Light prep</option><option value="standard">Standard cleanup packet</option><option value="heavy">Heavy cleanup packet</option></select><textarea name="notes" placeholder="What is messy about this client file?" style={{padding:12,minHeight:120}}/><input name="file" type="file" accept=".csv,.txt" style={{padding:12}}/><button type="submit" style={{padding:14,fontWeight:800}}>Generate packet JSON</button></form><p style={{marginTop:20,fontSize:13}}>Prep only. Final accounting decisions require professional review.</p></main>}