export default function IntakePage(){return <main className="wrap section"><div className="sectionIntro"><p className="kicker">Pre-ledger intake</p><h1 className="big">Create a cleanup prep packet.</h1><p className="muted">Use this before the client file is clean enough for QuickBooks or Xero. Upload raw CSV/TXT exports and describe what records are missing, mixed, stale, or unsupported.</p></div><form action="/api/cleanup-jobs" method="post" encType="multipart/form-data" className="card pad" style={{display:'grid',gap:16,maxWidth:760}}><label><span className="label">Firm name</span><input className="input" name="firmName" placeholder="Cleanup or catch-up bookkeeping firm" required/></label><label><span className="label">Reviewer email</span><input className="input" name="contactEmail" placeholder="reviewer@firm.com" type="email" required/></label><label><span className="label">Client / engagement name</span><input className="input" name="clientName" placeholder="Acme cleanup — 2024 catch-up" required/></label><label><span className="label">Prep type</span><select className="input" name="cleanupType" defaultValue="standard"><option value="starter">Starter packet — one account export</option><option value="standard">Standard prep — multiple accounts</option><option value="volume">Volume desk — recurring cleanup firm workflow</option></select></label><label><span className="label">What is messy before setup/import?</span><textarea className="input textarea" name="notes" placeholder="Example: missing March and April statements, mixed personal/business card, Stripe payouts not matched, prior bookkeeper export incomplete." required/></label><label><span className="label">Raw export</span><input className="input" name="file" type="file" accept=".csv,.txt" required/></label><button className="darkpill" type="submit" style={{border:0,justifySelf:'start'}}>Generate packet JSON</button><p className="small">This demo returns the packet as JSON. Production storage, reviewer queue, multi-file upload, and PDF export are next. Cleanup Desk prepares files for review; it does not make final accounting decisions.</p></form></main>}