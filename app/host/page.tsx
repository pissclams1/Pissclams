"use client";
import { FormEvent, useEffect, useState } from "react";
import Link from "next/link";
import { analyzeGap, money, prettyDate } from "../../lib/pricing";
import { saveListingRemote, type Listing } from "../../lib/store";
import { detectMarket, scoreGap, type MarketSignal } from "../../lib/market-intelligence";

type Signals = { rating?: string; reviews?: string; guests?: string; bedrooms?: string; bathrooms?: string; nightly?: string; city?: string; state?: string };
type Gap = { start: string; end: string; nights: number };
type ImportState = { imported?: boolean; source?: string; title?: string; description?: string; image?: string; note?: string; signals?: Signals; calendarOk?: boolean; gaps?: Gap[]; nextGap?: Gap | null; marketKey?: string; marketLabel?: string } | null;
type Advisor = { recommendation: "hold" | "package" | "split" | "salvage"; confidence: number; headline: string; reasoning: string[]; riskOfWaiting: string; nextAction: string; postCopy: string } | null;
type RangePlan = { startDate: string; endDate: string; nights: number; decision: "AIRBNB" | "FURNISHED"; confidence: number; reason: string };
type Plan = { days: { date: string; decision: "AIRBNB" | "FURNISHED"; confidence: number; reason: string }[]; ranges: RangePlan[]; summary: { airbnbDays: number; furnishedDays: number; airbnbPercent: number; furnishedPercent: number; headline: string } } | null;

export default function HostPage(){
  const [form,setForm]=useState({sourceUrl:"",startDate:"",endDate:"",nightlyTarget:"",expectedOccupancy:"50",cleaningFee:"150",market:"balanced",title:"",city:"",state:"",propertyType:"Home",bedrooms:"2",bathrooms:"1",hostName:"Host",ownerEmail:"",description:""});
  const [listing,setListing]=useState<Listing|null>(null);
  const [advisor,setAdvisor]=useState<Advisor>(null);
  const [saving,setSaving]=useState(false);
  const [importing,setImporting]=useState(false);
  const [imported,setImported]=useState<ImportState>(null);
  const [showManual,setShowManual]=useState(false);
  const [error,setError]=useState("");
  const [signal,setSignal]=useState<MarketSignal|null>(null);
  const [plan,setPlan]=useState<Plan>(null);

  useEffect(()=>{const sourceUrl=new URLSearchParams(window.location.search).get("url");if(sourceUrl){setForm(prev=>({...prev,sourceUrl}));void importSourceUrl(sourceUrl)}},[]);
  function update(key:string,value:string){setForm(prev=>({...prev,[key]:value}))}
  function gapNights(a:string,b:string){return Math.max(0,Math.round((new Date(`${b}T12:00:00`).getTime()-new Date(`${a}T12:00:00`).getTime())/86400000))}
  function refreshSignal(next:any){const startDate=next.startDate||form.startDate;const endDate=next.endDate||form.endDate;if(!startDate||!endDate)return;const marketKey=imported?.marketKey||next.marketKey||detectMarket(next.sourceUrl||form.sourceUrl,next.city||form.city,next.state||form.state);const daysUntil=Math.max(0,Math.round((new Date(`${startDate}T12:00:00`).getTime()-Date.now())/86400000));const s=scoreGap(startDate,gapNights(startDate,endDate),marketKey,daysUntil);setSignal(s);setForm(prev=>({...prev,expectedOccupancy:String(Math.round(s.fillProbability*100))}))}
  async function loadCalendarPlan(next:any){const sourceUrl=next.sourceUrl||form.sourceUrl;if(!sourceUrl)return;const response=await fetch("/api/calendar-plan",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({sourceUrl,city:next.city||form.city,state:next.state||form.state,horizonDays:365})});const data=await response.json().catch(()=>null);if(data?.ok)setPlan(data.plan)}

  async function importSourceUrl(sourceUrl=form.sourceUrl){
    setError("");setImporting(true);setAdvisor(null);setListing(null);setPlan(null);
    try{const response=await fetch("/api/import-listing",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({url:sourceUrl})});const data=await response.json();if(!data.ok){setImported(null);setShowManual(true);setError("We couldn’t import this listing automatically. Confirm the details below.");return}const s=data.signals||{};const next={sourceUrl,startDate:data.startDate||data.nextGap?.start||"",endDate:data.endDate||data.nextGap?.end||"",title:data.title||"",description:data.description||"",city:s.city||"",state:s.state||"",nightlyTarget:s.nightly||"",bedrooms:s.bedrooms||"2",bathrooms:s.bathrooms||"1",marketKey:data.marketKey};setImported(data);setForm(prev=>({...prev,sourceUrl,startDate:next.startDate||prev.startDate,endDate:next.endDate||prev.endDate,title:next.title||prev.title,description:next.description||prev.description,city:next.city||prev.city,state:next.state||prev.state,nightlyTarget:next.nightlyTarget||prev.nightlyTarget,bedrooms:next.bedrooms||prev.bedrooms,bathrooms:next.bathrooms||prev.bathrooms}));setShowManual(true);refreshSignal(next);await loadCalendarPlan(next)}catch{setImported(null);setShowManual(true);setError("We couldn’t import this listing automatically. Confirm the details below.")}finally{setImporting(false)}
  }
  function chooseGap(gap:Gap){setForm(prev=>({...prev,startDate:gap.start,endDate:gap.end}));setAdvisor(null);setListing(null);refreshSignal({...form,startDate:gap.start,endDate:gap.end})}
  function chooseRange(range:RangePlan){setForm(prev=>({...prev,startDate:range.startDate,endDate:range.endDate}));setAdvisor(null);setListing(null);refreshSignal({...form,startDate:range.startDate,endDate:range.endDate})}
  async function getAdvisor(analysis:any): Promise<Advisor>{const daysUntil=Math.max(0,Math.round((new Date(`${form.startDate}T12:00:00`).getTime()-Date.now())/86400000));const payload={listingTitle:form.title,city:form.city,state:form.state,propertyType:form.propertyType,bedrooms:Number(form.bedrooms)||2,bathrooms:Number(form.bathrooms)||1,rating:imported?.signals?.rating,reviews:imported?.signals?.reviews,sourceUrl:form.sourceUrl,gap:{startDate:form.startDate,endDate:form.endDate,nights:analysis.nights,daysUntil},economics:{nightlyRate:Number(form.nightlyTarget||0),likelyShortStayRevenue:analysis.likelyShortTermRevenue,furnishedOffer:analysis.recommendedTotal,monthlyEquivalent:analysis.monthlyEquivalent,fillProbability:analysis.fillProbability},marketSignal:signal?{marketLabel:signal.marketLabel,isPeakSeason:signal.isPeakSeason,nearHoliday:signal.nearHoliday,holidayName:signal.holidayName,reasoning:signal.reasoning,recommendation:signal.recommendation}:undefined};const response=await fetch("/api/ai-advisor",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(payload)});const data=await response.json();return data.advisor||null}
  async function submit(e:FormEvent){e.preventDefault();setSaving(true);setAdvisor(null);const analysis=analyzeGap({startDate:form.startDate,endDate:form.endDate,nightlyTarget:Number(form.nightlyTarget||0),expectedOccupancy:Number(form.expectedOccupancy||50),cleaningFee:Number(form.cleaningFee||0),market:form.market as "soft"|"balanced"|"hot",fillProbability:signal?.fillProbability,marketLabel:signal?.marketLabel});const aiAdvisor=await getAdvisor(analysis).catch(()=>null);const next:Listing={id:`listing-${Date.now().toString(36)}`,title:form.title||`Furnished stay in ${form.city||"your area"}`,city:form.city,state:form.state,propertyType:form.propertyType,bedrooms:Number(form.bedrooms)||2,bathrooms:Number(form.bathrooms)||1,hostName:form.hostName||"Host",description:form.description,amenities:["Fast Wi-Fi","Workspace","Kitchen","Laundry","Parking","Utilities included"],rules:["No smoking","Normal checkout cleaning","Quiet hours after 10 PM"],analysis,sourceUrl:form.sourceUrl||undefined,imageUrl:imported?.image,rating:imported?.signals?.rating,reviews:imported?.signals?.reviews,guestCount:imported?.signals?.guests,nightlyRate:imported?.signals?.nightly};try{const saved=await saveListingRemote(next, form.ownerEmail || undefined);setListing(saved);setAdvisor(aiAdvisor)}catch(error){setError(error instanceof Error?error.message:"Could not save this analysis.");setAdvisor(aiAdvisor)}finally{setSaving(false)}}

  return <main className="wrap section topspace"><div className="grid grid2"><div><p className="kicker">AI calendar CFO</p><h1 className="big">Every day gets one answer: Airbnb or furnished stay.</h1><p className="muted">GapStay builds a binary 365-day calendar plan so you can maximize annual cash without getting hypnotized by a few high-rate nights.</p><div className="card pad grid" style={{marginTop:24,marginBottom:18}}><Field label="Airbnb or VRBO listing URL" value={form.sourceUrl} onChange={v=>update("sourceUrl",v)} required={false}/><button className="darkpill" type="button" onClick={()=>importSourceUrl()} disabled={importing}>{importing?"Building calendar plan...":"Build my calendar plan"}</button><p className="small">Green means hold for Airbnb. Red means furnished stay. No ambiguity.</p></div>{error?<div className="errorBox">{error}</div>:null}{plan?<CalendarPlanView plan={plan} onPick={chooseRange}/>:null}{imported?<div className="card pad importedCard" style={{marginBottom:18}}><p className="kicker">{imported.calendarOk?`Calendar read — ${imported.gaps?.length||0} gap(s) found`:imported.imported?`Imported from ${imported.source||"listing"}`:"Manual entry ready"}</p><h2>{form.title||"Listing imported"}</h2>{imported.image?<img src={imported.image} alt={form.title}/>:null}<p className="small muted">{imported.note}</p>{imported.gaps&&imported.gaps.length>0?<div style={{display:"flex",gap:8,flexWrap:"wrap",marginTop:14}}>{imported.gaps.slice(0,6).map((g,i)=><button key={`${g.start}-${i}`} className="pill" type="button" onClick={()=>chooseGap(g)}>{prettyDate(g.start)} – {prettyDate(g.end)} · {g.nights}n</button>)}</div>:null}</div>:null}{signal?<div className="card pad" style={{marginBottom:18}}><p className="kicker">Selected range preview</p><h3>{signal.recommendation==="furnished"?"FURNISHED STAY":"AIRBNB"} · {Math.round(signal.fillProbability*100)}% short-stay confidence</h3><ul className="list">{signal.reasoning.map(r=><li key={r}>{r}</li>)}</ul></div>:null}{!showManual?<button className="manualLink buttonLink" type="button" onClick={()=>setShowManual(true)}>Enter details manually</button>:<form id="manual" onSubmit={submit} className="card pad grid" style={{marginTop:24}}><p className="kicker">Analyze selected range</p><div className="grid grid2"><Field label="Start date" type="date" value={form.startDate} onChange={v=>{update("startDate",v);refreshSignal({...form,startDate:v})}}/><Field label="End date" type="date" value={form.endDate} onChange={v=>{update("endDate",v);refreshSignal({...form,endDate:v})}}/></div><div className="grid grid3"><Field label="Nightly rate" value={form.nightlyTarget} onChange={v=>update("nightlyTarget",v)}/><Field label="Short-stay confidence %" value={form.expectedOccupancy} onChange={v=>update("expectedOccupancy",v)}/><Field label="Cleaning fee" value={form.cleaningFee} onChange={v=>update("cleaningFee",v)}/></div><div className="grid grid3"><Field label="City" value={form.city} onChange={v=>update("city",v)}/><Field label="State" value={form.state} onChange={v=>update("state",v)}/><Field label="Bedrooms" value={form.bedrooms} onChange={v=>update("bedrooms",v)}/></div><button className="darkpill" type="submit">{saving?"Asking AI advisor...":"Get AI recommendation"}</button></form>}</div><div className="card pad" style={{alignSelf:"start",position:"sticky",top:96}}>{listing?<Result listing={listing} advisor={advisor}/>:<p className="muted">GapStay is the CFO for your Airbnb calendar: it protects annual ROI by telling you which dates belong on Airbnb and which should become furnished housing.</p>}</div></div></main>
}
function Field({label,value,onChange,type="text",required=true}:{label:string;value:string;onChange:(v:string)=>void;type?:string;required?:boolean}){return <label><span className="label">{label}</span><input className="input" type={type} value={value} onChange={e=>onChange(e.target.value)} required={required}/></label>}
function Signal({label,value}:{label:string;value:string}){return <div className="stat"><span>{label}</span><strong>{value}</strong></div>}
function CalendarPlanView({plan,onPick}:{plan:NonNullable<Plan>;onPick:(range:RangePlan)=>void}){return <div className="card pad" style={{marginBottom:18}}><p className="kicker">365-day CFO plan</p><h2>{plan.summary.headline}</h2><div className="result" style={{marginTop:18}}><Signal label="Airbnb days" value={`${plan.summary.airbnbDays}`}/><Signal label="Furnished days" value={`${plan.summary.furnishedDays}`}/><Signal label="Airbnb %" value={`${plan.summary.airbnbPercent}%`}/><Signal label="Furnished %" value={`${plan.summary.furnishedPercent}%`}/></div><div style={{display:"grid",gridTemplateColumns:"repeat(30,1fr)",gap:3,marginTop:18}}>{plan.days.slice(0,360).map(d=><span key={d.date} title={`${d.date}: ${d.decision} · ${d.confidence}% · ${d.reason}`} style={{height:12,borderRadius:2,background:d.decision==="AIRBNB"?"#8fd19e":"#e68a75"}} />)}</div><div style={{marginTop:18,display:"grid",gap:10}}>{plan.ranges.slice(0,10).map(r=><button key={`${r.startDate}-${r.endDate}`} type="button" className={r.decision==="AIRBNB"?"pill":"darkpill"} onClick={()=>onPick(r)} style={{justifyContent:"space-between"}}><span>{prettyDate(r.startDate)} – {prettyDate(r.endDate)} · {r.nights} nights</span><b>{r.decision}</b></button>)}</div></div>}
function Result({listing,advisor}:{listing:Listing;advisor:Advisor}){const a=listing.analysis;return <div><p className="kicker">GapStay recommendation</p><h2 className="big">{advisor?.headline|| (a.recommendation==="furnished"?"Package this range":"Hold for Airbnb")}</h2><p className="muted">{prettyDate(a.startDate)} - {prettyDate(a.endDate)} · {a.nights} nights</p><div className="result" style={{marginTop:18}}><Signal label="Likely Airbnb" value={money(a.likelyShortTermRevenue)}/><Signal label="Furnished offer" value={money(a.recommendedTotal)}/><Signal label="Confidence" value={`${Math.round(a.fillProbability*100)}%`}/></div>{advisor?<div className="card pad" style={{marginTop:18}}><p className="kicker">Why</p><ul className="list">{advisor.reasoning.map(x=><li key={x}>{x}</li>)}</ul><p className="small"><b>Action:</b> {advisor.nextAction}</p><p className="small"><b>Post-ready copy:</b> {advisor.postCopy}</p></div>:<ul className="list">{a.explanation.map(x=><li key={x}>{x}</li>)}</ul>}<div className="actions"><Link className="darkpill" href="/pricing">Subscribe for monitoring</Link><Link className="pill" href={`/listing/${listing.id}`}>Preview offer</Link></div></div>}
